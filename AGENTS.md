# AGENTS.md

このファイルは、`D:\Apps\blender-scripts\addons\kurotori_blender_tools` で作業する開発エージェント向けのローカル指針です。

## リポジトリ前提

- このリポジトリは `blender-scripts/addons/kurotori_blender_tools` として配置されます。
- Blender 側では `D:\Apps\blender-scripts` を scripts directory として登録し、このフォルダ自体をアドオンとして認識させます。
- エントリーポイントは `__init__.py` です。Blender のアドオン一覧に表示されることを前提に変更します。

## 開発方針

- `register()` と `unregister()` の整合性を必ず維持してください。
- Blender API の登録対象を分割する場合でも、import 順序と依存方向を明確に保ってください。
- Blender 起動時やアドオン有効化時に例外を出さないことを優先してください。
- 既存ユーザー設定や保存データに影響する変更は、影響範囲を明示できる場合に限って行ってください。

## Python 環境

- 開発用 Python 環境は `uv venv` を前提にします。
- Blender 外での補完や静的解析には `fake-bpy-module` の利用を推奨します。
- 依存固定は初期段階では行わず、将来必要になった時点で `uv + pyproject optional-dependencies` を導入します。

## コーディング規約

- コメントは日本語で記述してください。
- 関数やクラスには、その言語に適した要約コメントを付けてください。
- コメントには、コードから自明ではない実装意図を書いてください。
- 修正履歴や作業ログをコードコメントやドキュメント内に残さないでください。
- Blender API に強く依存する処理と、純粋な Python ロジックは可能な範囲で分離してください。

## 変更時の注意

- このリポジトリ自体がアドオンとして読まれるため、トップレベルのモジュール構成変更は慎重に行ってください。
- 新しいファイルやサブパッケージを追加する場合は、`__init__.py` からの参照関係が破綻しないことを確認してください。
- README の機能一覧と使用方法は、ユーザー向けの挙動変更に合わせて更新してください。
